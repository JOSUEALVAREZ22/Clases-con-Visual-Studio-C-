Imports System
Imports ejercicio-semana5
'se crea una llamada a la clase import'
'se va a crear un objeto a partir de esta clase'
'se apuntara a una funcion de la clase'
Module Program
    Sub main(args As String())
        'se declara una variable'
        'definimos el tipo de As tipo objeto'
        'antes del nombre de la clase se debe usar new'
        Dim bcfor As New ejemplo_bucles()
        'static -> que se puede llamar sin instanciar la clase'
        bcfor.buclefor(10)


        'matodi main'
        'crear nuestro objeto'
        'dentro de est clase buscara el metodo'
        'ejecutara la funcion'

    End Sub
End Module
